[project]
name = "mnemosyne-integration-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "httpx>=0.28",
    "mcp>=1.26",
]

[tool.pytest.ini_options]
testpaths = ["."]
asyncio_mode = "auto"
